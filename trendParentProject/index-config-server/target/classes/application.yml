spring:
  application:
    name: index-config-server
  cloud:
    config:
      label: master
      server:
        git:
          uri: https://github.com/RuiZheng1103/MarketTrendSystem
          
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/